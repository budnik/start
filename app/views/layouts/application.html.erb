<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Help for Free – Choose your time and place to help." %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <h3 class="pull-left" style="color:#c80013">HELP FOR FREE</h3>
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%unless volunteer_signed_in?%>
            <%= link_to volunteer_omniauth_authorize_path('facebook'), class: ['btn','btn-info', 'brand'], id: 'fb-login' do%>
              <i class="icon-facebook-sign"></i>Увійти
            <%-end%>
          <%else%>
            <%= link_to me_path, class: [:brand, :thumbnail, :me] do %>
              <%= image_tag current_volunteer.image%>
            <%-end%>
          <%-end%>
          <div class="container pull-right nav-collapse">
            <ul class="nav">
              <li>
                <div style="padding-top:10px">
                  <div class="badge badge-success">2 in progress</div>&nbsp;<div class="badge badge-info">23 done</div>
                </div>
              </li>
              <li><%= link_to "John Smith", "/path1"  %>&nbsp;</li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
      <%= bootstrap_flash %>
      <%= yield %>
    <hr>
    <footer>
      <p>&copy; Company 2013</p>
    </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
